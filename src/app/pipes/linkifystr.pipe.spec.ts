import { LinkifystrPipe } from './linkifystr.pipe';

describe('LinkifystrPipe', () => {
  it('create an instance', () => {
    const pipe = new LinkifystrPipe();
    expect(pipe).toBeTruthy();
  });
});
